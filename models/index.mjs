// Importing the User model from user.mjs
import User from "./user.mjs";

// Exporting an object containing the User model
export default {
    User, // User model is available for use in other parts of the application
};
